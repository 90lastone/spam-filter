export function isSpam(
  content: string, spamLinkDomains: string[], redirectionDepth: number
): boolean {
  // TODO content 안에 spamLinkDomains 가 하나라도 포함되어있으면 true 를 리턴한다.

  /*
  TODO 없으면 redirectionDepth 만큼 리다이렉트하는동안 (* 내용 확인 필요)
  리다이렉트 된 페이지 내 content 중 spamLinkDomains 가 하나라도 포함되어있으면 true 를 리턴한다.
   */

  return true;
}